contents:
  keyring:
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - https://packages.wolfi.dev/os
    - "@local /packages"
  packages:
    - smartctl-exporter@local

entrypoint:
  command: /usr/bin/smartctl_exporter

archs:
  - amd64
  - arm64
